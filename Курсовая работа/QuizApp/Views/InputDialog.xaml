<Window x:Class="QuizApp.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ввод" Height="150" Width="400"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Text="Введите значение:" Margin="0,0,0,5"/>
        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="0,0,0,10" Padding="3"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OK_Click" Margin="0,0,10,0" Padding="20,5" IsDefault="True"/>
            <Button Content="Отмена" Click="Cancel_Click" Padding="20,5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>