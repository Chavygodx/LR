<Window x:Class="QuizApp.Views.QuizPassWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Прохождение викторины" Height="400" Width="600"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="ProgressText" Text="Вопрос 1 из 5" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <TextBlock x:Name="QuestionTextBlock" Grid.Row="1" TextWrapping="Wrap" FontSize="16" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="2" x:Name="OptionsPanel" Margin="0,0,0,10">
            <!-- RadioButtons будут создаваться динамически -->
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="AnswerButton" Content="Ответить" Click="AnswerButton_Click" Margin="0,0,10,0" Padding="20,5"/>
            <Button x:Name="NextButton" Content="Далее" Click="NextButton_Click" Padding="20,5" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>